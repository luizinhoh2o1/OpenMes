# OpenMES — Docker Compose environment variables
#
# Copy this file to .env and adjust the values:
#   cp .env.example .env
#
# This file configures Docker Compose variable substitution.
# It is NOT the Laravel .env (that lives in backend/.env).

# ── Database ──────────────────────────────────────────────────────────────────
# Shared between the postgres container and the backend container.
POSTGRES_DB=openmmes
POSTGRES_USER=openmmes_user
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# ── Application ───────────────────────────────────────────────────────────────
APP_NAME=OpenMES
APP_URL=http://localhost
APP_PORT=80

# ── Admin account (created automatically on first run) ────────────────────────
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=CHANGE_ME_SECURE_PASSWORD
