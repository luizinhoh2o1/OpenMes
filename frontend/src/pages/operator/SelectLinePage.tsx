import { Container } from '@mantine/core';
import { LineSelector } from '../../components/operator/LineSelector';

export const SelectLinePage = () => {
  return (
    <Container size="md" py="xl">
      <LineSelector />
    </Container>
  );
};
